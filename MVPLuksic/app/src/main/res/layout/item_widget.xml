<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/Widget.ListaLuksic.AppWidget.Container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:theme="@style/Theme.ListaLuksic.AppWidgetContainer">

    <TextView
        android:id="@+id/appwidget_text"
        style="@style/Widget.ListaLuksic.AppWidget.InnerView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="8dp"
        android:layout_marginBottom="8dp"
        android:contentDescription="@string/app_widget_description"
        android:text="@string/appwidget_text"
        android:textSize="12sp"
        android:textStyle="bold|italic" />

    <ImageView
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:id="@+id/appwidget_image"
        android:scaleType="centerCrop"
        android:layout_below="@id/appwidget_text"
        android:layout_centerHorizontal="true"/>
</RelativeLayout>